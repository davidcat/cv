name: Deploy Resume to GitHub Pages

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Pages
      uses: actions/configure-pages@v4

    - name: Build HTML Resume
      run: |
        echo '<!DOCTYPE html>' > index.html
        echo '<html lang="zh-CN">' >> index.html
        echo '<head>' >> index.html
        echo '<meta charset="UTF-8">' >> index.html
        echo '<meta name="viewport" content="width=device-width, initial-scale=1.0">' >> index.html
        echo '<title>张三 - iOS 开发工程师简历</title>' >> index.html
        echo '<link rel="stylesheet" href="style.css">' >> index.html
        echo '</head>' >> index.html
        echo '<body>' >> index.html
        echo '<div class="container">' >> index.html
        echo '<header>' >> index.html
        echo '<h1>故知</h1>' >> index.html
        echo '<div class="contact-info">' >> index.html
        echo '<p>📱 15214363830 | ✉️ shawn2045@qq.com | 📍 深圳市</p>' >> index.html
        echo '<p><a href="https://linkedin.com/in/guzhi">LinkedIn</a> | <a href="https://github.com/guzhi">GitHub</a></p>' >> index.html
        echo '</div>' >> index.html
        echo '</header>' >> index.html
        echo '<section class="summary">' >> index.html
        echo '<h2>🎯 个人简介</h2>' >> index.html
        echo '<p>资深 iOS 开发工程师，具有 10 年移动应用开发经验。精通 Objective-C 和 Swift 编程，熟悉主流开源框架。拥有金融保险、工业互联网领域工作经历，同时掌握 HarmonyOS 开发。</p>' >> index.html
        echo '</section>' >> index.html
        echo '<section class="experience">' >> index.html
        echo '<h2>💼 工作经历</h2>' >> index.html
        echo '<div class="job">' >> index.html
        echo '<h3>iOS 工程师 | 深圳市新源润产品信息技术有限公司</h3>' >> index.html
        echo '<p class="date">2023年2月 - 至今 | 深圳</p>' >> index.html
        echo '<ul>' >> index.html
        echo '<li>负责公司 iOS 应用开发和维护工作</li>' >> index.html
        echo '<li>主导技术选型，制定开发规范和最佳实践</li>' >> index.html
        echo '<li>负责新人技术指导，推动团队技术能力提升</li>' >> index.html
        echo '<li>编写技术文档，建立团队知识库</li>' >> index.html
        echo '</ul>' >> index.html
        echo '</div>' >> index.html
        echo '<div class="job">' >> index.html
        echo '<h3>iOS 开发工程师 | 天瑞集团</h3>' >> index.html
        echo '<p class="date">2020年1月 - 2023年1月 | 深圳</p>' >> index.html
        echo '<ul>' >> index.html
        echo '<li>主导智慧矿山工业互联网项目开发</li>' >> index.html
        echo '<li><strong>业绩：</strong>项目入选工信部2022年工业互联网试点示范项目</li>' >> index.html
        echo '<li>性能优化：启动时间优化30%，内存使用降低25%</li>' >> index.html
        echo '</ul>' >> index.html
        echo '</div>' >> index.html
        echo '</section>' >> index.html
        echo '<section class="skills">' >> index.html
        echo '<h2>🛠️ 技术栈</h2>' >> index.html
        echo '<div class="skill-tags">' >> index.html
        echo '<span class="tag">Objective-C</span><span class="tag">Swift</span><span class="tag">HarmonyOS</span>' >> index.html
        echo '<span class="tag">AFNetworking</span><span class="tag">Alamofire</span><span class="tag">SnapKit</span>' >> index.html
        echo '<span class="tag">SDWebImage</span><span class="tag">Masonry</span><span class="tag">Kingfisher</span>' >> index.html
        echo '<span class="tag">Moya</span><span class="tag">RealmSwift</span><span class="tag">WebSocket</span>' >> index.html
        echo '</div>' >> index.html
        echo '</section>' >> index.html
        echo '</div>' >> index.html
        echo '</body>' >> index.html
        echo '</html>' >> index.html

    - name: Upload Pages artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4